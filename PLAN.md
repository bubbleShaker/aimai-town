# 曖昧な町（aimai-town）開発計画

スマホブラウザで遊ぶ、ノード型マップ探索の思索的謎解きゲーム。
GitHub Pages 公開: https://bubbleshaker.github.io/aimai-town/

## 1. 何を作るのか

### 原典となる思想

> 他者を介在させずに自立し、承認に囚われずに幸福を得ている状態を人間の真の幸せだと今のところ認識している。
> しかし人は一人では生きられず、社会生活では他者との関わりと尊重が求められる。
> 仕事では自律しつつ他者を尊重し目的を遂行することが大切。
> 一方で趣味や雑談は目的が明確でなく、関係性の構築自体が目的の一つになっている。
> 人と関われば関わるほど、「他者を介在させない幸福」からは離れて見え、矛盾が生じているように見える。
> その矛盾を言語化するのは難しく、人々は曖昧に感覚的に生きているのかもしれない。
> 答えが一つに収束するものとも思わない。幸せの感じ方は生まれ持った特性に依る部分が大きいから。

### ゲームとしての変換

**謎解きの対象は主人公自身の深層心理**とする。開発者側に「正解」は用意しない。

- プレイヤーは霧の町を巡り、住人との対話から **信念の断片**（短い命題テキスト）を集める。
- 町の要所には **扉** がある。扉は「両立しないように見える2つの断片」を提示し、
  **その間に橋を架ける第三の断片**を選ぶことを要求する。
- 橋の架け方は**常に複数正解**。どう架けたかが軌跡として記録され、エンディングを分岐させる。

「正解を当てるゲーム」ではなく「**自分の答えを選んだ痕跡が残るゲーム**」にする。

## 2. 世界観

町の名は **曖昧（あいまい）**。霧に沈み、住人は各々の「灯」を掲げて暮らしている。
灯は幸福の形の象徴で、色も明るさも人によって違う。
主人公は名も性別も持たない **「あなた」**。自分の灯が何色か分からないまま町に降りてくる。

### 場所（ノード）

| ノード | 象徴するもの |
| --- | --- |
| 広場 | 目的のない雑談。起点 |
| 織り小屋 | 仕事。自律と協調の両立 |
| 酒場 | 趣味・関係構築 |
| 喫煙所 | 離れるために出てきた者どうしが並ぶ場所。火だけは借りないと点かない |
| 水汲み場 | 話が生まれるのを待って並ぶ場所。水は、話すための気力 |
| 井戸 | 内省。自分の声だけが返る |
| 橋 | 自分と他者の境界 |
| 灯台 | 完全な孤独における幸福 |
| 霧の底 | 終幕。灯を選ぶ場所 |

喫煙所と水汲み場には扉を付けていない。ここは矛盾を突きつける場所ではなく、
**矛盾が日常の姿で置いてある場所**だから（扉を四つ目にすると、三つの扉＝三つの矛盾という
構図と終幕の条件が変わる）。そのぶん素通りもできるが、どちらにも断片があるので、
寄らずに終わると隠しの終幕には届かない。

広場を挟んで、右手が喫煙所（離れるために出てくる場所）、左手が水汲み場（並びに来る場所）。
同じ「輪の外」でも向きが逆なので、町の形としても左右に振り分けてある。

水汲み場が抱えるのは、話しかけることの不確実性。
黙っている人が考えているのか何もしていないのかは、こちらからは分からない。
声をかければ誰かの考えを途切れさせ、黙っていれば何も始まらないまま順番が来る。
**水は、話すための気力**として置いてある。汲みに来るのは、離れることが目的ではないから。

### 住人（NPC）

- **灯台守** — 誰も介さず幸福を完結させていると語る。ただし語る相手を必要としている
- **織り手の親方** — 仕事において自律と尊重を両立させている実務家
- **酒場の常連** — 「無目的であることが目的だ」と言う
- **子ども** — 承認をまっすぐ求める。取り繕わない
- **一服する人** — 輪から離れるために出てきて、離れた先で誰かと並んでいる
- **前に並ぶ人** — 黙って立っている。考えているのかどうかは、最後まで分からない
- **水番** — 目上。問いには必ず答え、そこで終わる。捻り出した問いだと、たぶん分かっている
- **水を汲む人** — 話が生まれる場所と時間を選んでいる。冷たい決め方だと自分でも知っている
- **名もなき人** — プレイヤーの選択を映し返す鏡

## 3. ゲーム機構

### 2つの軸

プレイヤーの選択は隠しパラメータ2軸を動かす。数値はプレイヤーに見せない。

- **距離軸** `solitude ←→ connection` — 幸福を一人で完結させるか、関わりの中に置くか
- **確信軸** `certainty ←→ ambiguity` — 答えを持つか、曖昧なまま生きるか

### 三つの扉

| 扉 | 両立しない2つの断片 |
| --- | --- |
| 仕事の扉 | 「自律して動く」× 「他者の言動を尊重する」 |
| 雑談の扉 | 「目的のない時間は空虚だ」× 「関係を築くために雑談する」 |
| 幸福の扉 | 「他者を介さぬことが真の幸福」× 「人は一人では生きられない」 |

各扉には橋となりうる断片が複数あり、どれを選んでも開く。選んだ断片が軸を動かす。

### エンディング（6種）

| # | 名 | 条件 |
| --- | --- | --- |
| 1 | 灯台の灯 | 孤独 × 確信 |
| 2 | 織り手の手 | 関わり × 確信 |
| 3 | 広場の灯 | 関わり × 曖昧 |
| 4 | 霧のまま | 孤独 × 曖昧 |
| 5 | なかばの灯 | どちらにも振れなかった（軸のどちらかが 0） |
| 6 | 名もなき灯（隠し） | 全断片収集 かつ 三つの扉すべてで、突きつけられた二枚の片方を置いた |

5 は当初 4 に含めていたが、「決めきらなかった」ことを孤独と言い切るのは違うので独立させた。
どちらの軸で止まったかは、その文では名指ししない（片方を名指すと、もう片方で止まった人に嘘になる）。

6 は「間に第三の橋を架けず、矛盾する当の一枚をそのまま置いた」＝矛盾を矛盾のまま抱えた、という読み。

どのエンドも優劣なく肯定される。「正解のエンド」は作らない。
終幕の画面では、何番目のエンドかも、いくつあるかも出さない（出した瞬間に「集めるもの」になるため）。

## 4. 技術構成

- Vite + React + TypeScript / GitHub Pages（Actions でビルドしてデプロイ。`master` に入ると公開される）
- スマホ縦持ち・タップ前提の **DOM ベース UI**（Canvas は使わない。テキストが主役のため）
- セーブは `localStorage`
- **シナリオデータとエンジンを完全分離**する。
  `src/scenario/` は純粋なデータ定義のみ、`src/engine/` はそれを解釈する純粋関数のみ、
  `src/ui/` が React コンポーネント。世界観を書き足す作業とコードを触る作業を分離する。

```
src/
  scenario/    思想・テキスト・断片・扉・エンド定義（データのみ / React 非依存）
  engine/      状態遷移の純粋関数（軸の計算・扉の解決・エンド判定）
  ui/          React コンポーネント
  store/       localStorage 永続化
```

## 5. マイルストーン

- [x] **M0** プロジェクト雛形・型定義・PLAN
- [x] **M1** ノードマップ探索と対話の最小ループ（断片が集まる）… #1 / PR #2
- [x] **M2a** 扉の仕組みと最初の扉（仕事の扉）… #3 / PR #4
- [x] **M2b** 残る二つの扉と、そのための場所・住人（酒場・橋・灯台）… #5
- [x] **M3a** 終幕の場所「霧の底」と、軸の集計・エンディング分岐… #7
- [x] **公開** GitHub Pages に出す（M4 から前倒し）… #9
- [x] **M3b** エンドロール（扉に何を置いたかの軌跡）と、始め直し… #11
- [x] **M4a** セーブ（localStorage 永続化と、壊れた保存の読み戻し）… #13
- [x] **M4b** 文字送り（一字ずつ送り、触れると一気に出す）… #15
- [x] **M4c** 演出（霧・灯）… #17
- [x] **M5** 周回要素（一度読んだ言葉で待たせない）… #21
      ※ 集めた断片の一覧（思索のノート）は M4a の途中で先に入っていた
- [x] **M6** 音（町に流れる一曲と、止める口）… #23
- [x] **町を足す** 喫煙所（離れることと、火を借りること）… #27
- [x] **町を足す** 水汲み場（話しかけることの不確実性と、身の置き所）… #29

積み残し（急がない）: 連打・キーリピートで読まないまま場面を通過できる… #18

M4「演出とセーブ」は性質が違うものが混ざっていたので分けた。
セーブだけがデータ構造とテストの話で、あとから演出を足しても壊れない土台になる。

## 6. セーブの決めごと

保存するのは `GameState` だけ。場面（Scene）は保存しない。
「対話の何行目か」を復元しても意味が無く、`GameState` をそのまま載せる形も崩れるため。

保存の中身は信用しない。手で書き換わるし、世界データを書き足した後に古い保存が残る。
読み戻すときの規則は次のとおりで、`engine/restore.ts` に置いてある（副作用は `store/save.ts` だけ）。

- 世界に無い場所・断片・対話・扉を指す記録は落とす。歩みそのものは残す
- 立っている場所が世界から消えていたら、保存ごと捨てる
- 開いた扉と、そこに置いた一枚の記録は対で残す。片側だけ残さない
- まだ入れない場所に立った保存は、始まりの場所へ戻す
- まだ何もしていない状態は保存しない（降りる場面を読みかけで閉じた人に、その文を飛ばさせない）

## 7. 文字送りの決めごと

最後の一行だけを一字ずつ送る。触れるとその行を出し切り、出し切ってから触れると次へ進む。
送った字数は表示の都合なので `GameState` にも `Scene` にも置かず、`ui/StoryView.tsx` の中だけで持つ。

守るのは「読ませないまま先へ行かせない」の一点で、そこから次が決まっている。

- どこまで送ったかは、場面の実体・行番号・文面の三つで見分ける（`ui/typewriter.ts`）。
  行が変わった一瞬だけ前の行の字数が当たると、そこで触れた人は一字も読まずに次の場面へ行ける
- 下端を追うかどうかは、動いた先がどこかだけで決める（`ui/follow.ts`）。
  指かホイールかで見分けると、位置の動かない入力で追うのをやめたきり戻らない
- 進むと決めた触れ方では、追うのを必ず戻す。読み返しで離れたまま行が増えると、
  増えた行は画面の外に出たままになり、何も起きていないように見えて叩き続けることになる
- 言葉を選んでいるあいだは進まない。本文は選んで読み返せるようにしてあるため
- 送りかけの文は目にだけ見せ、読み上げには全文を渡す（欠けた文は言葉にならない）。
  読み上げを急かさないよう `aria-live` は付けない
- `prefers-reduced-motion: reduce` の人には送らず、はじめから全文を出す

上の三つ目は絵に映らないので、`scripts/shoot.cjs` の `assertNotLeftBehind` で通しプレイ中に検める。

まだ決めていないこと: 一行につき二回の入力を数えているだけなので、連打やキーリピートでは
読まないまま通過できる。待たせて塞ぐかどうかは #18 で預かっている
（「急ぐ人には一息で」と向きが逆になるため、入れないという判断もある）。

## 8. 演出の決めごと

演出は `src/index.css` の中に閉じる。`GameState` にも `Scene` にも持ち込まない。
`ui/MapView.tsx` が渡すのは「まだ知らない場所か」の一つだけで、判定はすべて engine の値から出す。

- **灯の色をプレイヤーの選択で変えない。** 途中の色が、終幕で選ぶ灯の示唆になってはいけない
- **軸を見せない。** 隠しパラメータの可視化に演出を使わない
- **沈めるのは滲みではなく光量で。** ぼかすと灯のにじみが広がり、
  遠い灯のほうが大きく明るく見えて、近づいた場所と逆転する
- **歩ける先と、閉ざされた戸は沈めきらない。** 行けること・閉ざされていることまで隠すと、
  町の形が読めなくなる（`.place.is-far` の例外はこの二つだけ）
- **`prefers-reduced-motion: reduce` では名指しで止めない。**
  止めるものを並べると、あとから演出を足した人が書き足すのを忘れる。
  `*` で一括して `animation` も `transition` も止める。要素そのものの見た目は残るので、
  霧も灯も「動かないだけ」で消えはしない

霧が流れているかも、動きを減らす設定で静止するかも、一枚の絵には映らない。
`scripts/shoot.cjs` の `assertFogDrifts` / `assertStillWhenReduced` が通しプレイ中に検める。

## 9. 周回の決めごと

二周目の人に、一度読んだ言葉を一字ずつ送り直さない。
**飛ばすのではなく、待ち時間だけを消す。** 既読の読み物ははじめから全文を開くので、
全文はその場に残り、読み返しながら考えられる。

これは「読ませないまま先へ行かせない」を破らない。守るべきは「一度は目にさせる」ことで、
既読とはそれが果たされたということだから。

- **持ち越すのは「一度読んだか」だけ。** 集めた断片も、戸に置いた選択も、引いた終幕も持ち越さない。
  蓄積を作ると、この町が「集めきるもの」に変わる（隠しの終幕が全断片収集なので、とくに）。
  なお戸の返答は `reply:<戸>:<断片>` という名で覚えるので、前の周にどの戸へ何を置いたかは
  記録の中に残る。遊びには一切効かない（送り方を決めるのにしか読まない）。
  ここを別の用途に使うと、持ち越さないと決めたものが持ち越されはじめる
- **歩みの保存とは別の鍵に書く。** 始め直しは `aimai-town/save` を消す。
  同じ鍵に相乗りさせると、始め直した瞬間に既読が消えて周回を越えられない。
  消す口は用意しない（始め直しても残るのが役目だから）
- **既読を一覧や印では出さない。** 「まだ読んでいないもの」が名指しされると、それが集める対象になる。
  ただし送り方の違いとしては必ず現れる（二周目には、送られてくる言葉が未読だと一目で分かる）。
  待ち時間を消す以上これは避けられないので、そこは受け入れる。
  「だから既読の印を足してもよい」とは読まないこと
- **覚えるのは最終行まで読み切って次へ進んだ瞬間だけ。**
  途中で閉じた人を覚えると、次に開いたとき一行も目にしないまま通過できる
- **記録するのは「その読み物を読んだか」で、「その文面を読んだか」ではない。**
  言葉を書き換えても id が同じなら既読のまま一息で出る。全文はその場に出るので読めるし、
  文面を鍵にすると、一字直すたび町じゅうが未読に戻る
- **世界に無い名が混じっていても捨てない。** 通してもルールは破れず、
  世界の言葉を書き足す前に読んだ記録まで流す理由も無い（`engine/restore.ts` とは向きが違う）。
  際限なく膨らむのを防ぐ上限だけ置いてある

一字ずつ送られたかは一枚の絵に映らない（送り終えた画面と、はじめから出し切った画面は同じ姿）。
`scripts/shoot.cjs` の `assertTypedForNewWords` / `assertNoWaitOnReread` が通しプレイ中に検める。

## 10. 音の決めごと

町に流れるのは一曲だけ。素材は MusMus の `old Gold`（watson 作）を頂いた。
演出の決めごとの延長にあるので、**音は世界の状態を映さない。**

- **曲は場面で変えない。** 場所でも軸でも終幕でも切り替えない。
  灯の色を選択で変えないのと同じ理由で、音が隠しパラメータの示唆になってはいけない。
  そのため `ui/useBgm.ts` は `GameState` も `Scene` も受け取らない（渡す道を作らない）
- **自動では鳴らさない。** 最初の触れで、無音から四秒かけて立ち上げる。
  ブラウザが触れる前の再生を止めるからでもあるが、開いた途端に鳴らして、
  一行目を読む前に驚かせないためでもある。
  ただし四秒かけるのは初めの一度だけ。自分で押して鳴らし直した人には手応えが要るので、
  そちらは止めるときと同じ速さで戻す
- **鳴らし始めは、触れを受け取ったその場から同期で呼ぶ。**
  組む・起こす・鳴らすを state に預けて effect でやると、iOS では鳴らない。
  あちらは「いまの呼び出しが人の触れから続いているか」を呼び出し元の連なりで見るので、
  一度画面を描いてから呼び直したものは触れの続きと認められない。
  拒まれ続けて、♪ は点いたまま二度と鳴らない画面になる（issue #25）。
  触れた回数を数えて effect へ渡す作りに戻さないこと。
  止める側は逆に、消えるのを待つ必要があるので移り変わりを見て動かす
- **鳴り出すまでは、触れるたびに試し直す。** 最初の触れを「活性化」と認めない端末がある
  （iOS の見立ては厳しい）。一度で諦めると、♪ は点いたまま二度と鳴らない画面になる。
  押した側と離した側の両方を聞くのも同じ理由（どちらを認めるかが端末で違う）。
  端末の側で止められたときも、次に触れれば鳴り直す
- **止めている人には、音を取りに行かせない。** 素材は 1.4MB ある。
  先読みも、音の通り道を組むこともしない（iOS では、口を開けるだけで他のアプリの音を止めうる）
- **止める口は必ず出す。** 場面によって隠さない。隠すと、止めたい人が止められる場面を探して歩く
- **音量は `audio.volume` ではなく Web Audio の GainNode で絞る。**
  iOS は `volume` への代入を黙って無視するので、そこに頼ると iOS の人にだけ
  立ち上がりが効かず、最初の触れでいきなり鳴る
- **記憶は歩みとも既読とも別の鍵（`aimai-town/sound`）に書く。**
  歩みに相乗りさせると、始め直した瞬間に音の設定まで戻る。
  読めなければ既定（鳴らす側）に戻す。世界の形に依らないので、検めるのは真偽値かどうかだけ
- **止める口を押しても物語は進めない。** 音を止めようとした人が一行読み飛ばされるのは、
  「読ませないまま先へ行かせない」を音の側から破ることになる

- **音の要素と、止める口と、鳴らし方を一枚（`ui/Sound.tsx`）に閉じる。**
  音の要素だけを外に出すと、`loop` を落とすような書き換えを誰も止められない
- **立ち上げるのは、鳴らそうとした時ではなく鳴り出した時から。**
  呼んだ時から傾きを引くと、遅い回線では無音のまま四秒が過ぎて全開で鳴り出す。
  拒まれた端末では、その一度で「もう鳴らした」ことになり、初めの一音が短い立ち上がりにすり替わる
- **通り道は畳まない（`close()` しない）が、止めているあいだは眠らせる（`suspend()`）。**
  畳むと、同じ audio 要素に源は一度しか作れないので組み直せず、二度と鳴らない画面になる。
  かといって起こしたままだと、止めた人の端末が音の口を掴み続ける。
  眠らせるのは消え切ってから（眠っているあいだは時が進まず、消しかけだとその高さで凍る）

鳴っているかは一枚の絵に映らない。`scripts/shoot.cjs` の `assertSilentBeforeTouch` /
`assertSounding` / `assertToggleStopsSoundOnly` が通しプレイ中に検める。
見るのは動いているか（`paused`）だけでなく、絞りの高さまで。
絞りが 0 に貼り付いても audio は動いたままで、それがいちばん分かりにくい壊れ方だから。
