# 曖昧な町 — 開発の作法

スマホブラウザ向けの、ノード型マップ探索＋思索的謎解きゲーム。
Vite + React 19 + TypeScript / GitHub Pages。

**現在地とマイルストーンは [PLAN.md](./PLAN.md) を見る。**
**テキストを書くときは [src/scenario/VOICE.md](./src/scenario/VOICE.md) を先に読む。**

## この作品の核

原典の思想は「他者を介在させない自立した幸福」と「社会生活で求められる関わり」の矛盾。
**開発者側に正解を用意しない。** これは方針ではなく、実装の制約として守る。

- 扉は**どの断片を差し出しても開く**。開かない選択肢を作らない。
  変わるのは扉が返す言葉と、記録される選択だけ。
- エンディングに優劣をつけない。「正解のエンド」を作らない。
- この作法は `src/engine/state.test.ts` の「どの断片を差し出しても扉は開き、選択が記録される」で
  担保している。テストが落ちたら、仕様ではなく企画が壊れたと考える。

## 層の分離

```
src/
  scenario/  世界のデータのみ（React にも DOM にも依存しない）
  engine/    状態遷移の純粋関数。副作用なし
  store/     localStorage への読み書き。副作用はここだけ
  ui/        React コンポーネント。描画だけ
```

依存は `scenario → engine → ui` の一方向。逆流させない。
`store/` は engine を使うが、engine から store は見ない（engine を副作用なしに保つため）。
世界観を書き足す作業は `scenario/` だけで完結させる。

## 実装の約束事

レビューで固まったもの。理由まで含めて守る。

- **ルールは engine に一本化する。** 移動可否・断片の獲得・扉の開閉の判定を UI 側で再実装しない。
  UI の不備でルールが破られないようにするため。
- **変化を起こす行動は engine に投げ、その結果を見てから画面を決める。**
  `reduce` が拒否したら何も起きなかったことにする（同じ state が返る）。
  「読ませ切ってから dispatch して、成否を確認せず次へ進む」を書かない。
- **世界データの不変条件はテストで縛る。** id の重複、扉の行き先、
  「必ず差し出せる断片には返答がある」など。
  シナリオを書き足す人がコードを読まなくて済むようにするため。
- **Scene は id ではなく実体を持つ。** 引き直しに失敗して「何も描かれず操作もできない」
  状態になる経路を、型で消す。
- **分岐は網羅性チェック（`never`）で守る。** `AfterReading` に枝を足したとき、
  書き忘れが黙って `default` に落ちないようにする。
- **`GameState` には表示の都合を入れない。** 「対話の何行目か」は Scene 側。
  `GameState` はそのまま localStorage に載る形を保つ。
- **保存の中身は信用しない。** 手で書き換わるし、世界データを書き足したあとに古い保存が残る。
  読み戻しは `engine/restore.ts` で検めてから `GameState` にする。
  とくに「まだ入れない場所に立った保存」を通すと、扉を開かずに終幕へ行ける。
  規則は PLAN.md「セーブの決めごと」にまとめてある。
- **ルールの担保は、記録の件数ではなく世界の側で数える。**
  `heldContradiction` が数えるのは町の戸の数。記録の件数と突き合わせると、
  記録が欠けたぶんだけ条件を満たしたことになってしまう。
- **選択は集計値ではなく生で記録する。** 軸の重みを後から変えても、
  過去の記録の意味が変わらないようにするため。
- **読ませないまま先へ行かせない。** 一字送り・下端の追従・選択中の扱いは、
  どれも「触れた人が一行も目にしないまま次の場面へ行ける」経路を塞ぐためにある。
  文字が絡む見た目を触るときは PLAN.md「文字送りの決めごと」を先に読む。
  絵に映らない崩れ方をするので、`scripts/shoot.cjs` の `assertNotLeftBehind` で通しプレイ中に検めている。
- **周回に持ち越すのは「一度読んだか」だけ。** 歩みの保存とは別の鍵（`aimai-town/lore`）に書く。
  始め直しは `clearSave()` を呼ぶので、同じ鍵に相乗りさせると既読が消えて周回を越えられない。
  断片や選択を持ち越すと、この町が「集めきるもの」に変わる。決めごとは PLAN.md「周回の決めごと」。
- **演出は CSS の中に閉じる。** 霧も灯も `src/index.css` だけで動かし、`GameState` にも `Scene` にも
  演出の都合を持ち込まない。灯の色は選択で変えず、隠しパラメータを演出で見せない。
  `prefers-reduced-motion: reduce` では止めるものを名指しせず `*` で一括して止める
  （名指しだと、演出を足した人が書き足すのを忘れる）。決めごとは PLAN.md「演出の決めごと」にある。

## 確認のしかた

```bash
npm test        # engine の単体テスト。世界データの整合性もここで落ちる
npm run build   # 型チェックとビルド
npm run lint
```

スマホ幅（375×667）での通しプレイを撮る:

```bash
npm run dev -- --host 127.0.0.1 --port 5199   # 別ターミナルで
node scripts/shoot.cjs <出力先ディレクトリ>
```

この WSL 環境では `page.screenshot()` がフォント待ちで固まるため、
`scripts/shoot.cjs` は CDP の `Page.captureScreenshot` から撮っている。
Playwright の click は `{ force: true }` を付ける（actionability 判定で止まるため）。

## 見た目の制約

- スマホ縦持ちが前提。`100dvh` と `env(safe-area-inset-bottom)` を使う
- スクロールする領域の中に、閉じる／離れるボタンを入れない
- 下寄せのスクロール領域に `justify-content: flex-end` を使わない
  （あふれた先頭側に到達できなくなる。`:first-child { margin-top: auto }` を使う）
- タップを繰り返す面には `touch-action: manipulation` と `user-select: none`
